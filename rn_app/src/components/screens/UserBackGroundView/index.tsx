import React from 'react';
import {StyleSheet, View} from 'react-native';

type Props = {};

export const UserBackGroundView = ({}: Props) => {
  return <View />;
};

const styles = StyleSheet.create({
  container: {},
});
